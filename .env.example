# shellcheck shell=bash
# Identity and signing values are filled from the chezmoi profile into ~/.env.
# See identity.git_email / identity.git_ssh_key / secrets.git_gpg_key.
# The rendered file is ~/.env (create_private_dot_env.tmpl).
