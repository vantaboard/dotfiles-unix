abbr "2stem"="poetry -C /home/brighten-tompkins/Code/spleeter run spleeter separate -o /home/brighten-tompkins/StemMusic -p spleeter:2stems -f '{filename}_{instrument}_2spleeter.{codec}'"
abbr "5stem"="poetry -C /home/brighten-tompkins/Code/spleeter run spleeter separate -o /home/brighten-tompkins/StemMusic -p spleeter:5stems -f '{filename}_{instrument}_5spleeter.{codec}'"
abbr "automt"="sudo mount -a"
abbr "bql"="bq load --source_format=CSV --skip_leading_rows=1"
abbr "bqq"="bq query --use_legacy_sql=false -n 10 <"
abbr "bqq5"="bq query --use_legacy_sql=false -n 5 <"
abbr "cast"="gnome-network-displays"
abbr "cfg"="move_to_config_env"
abbr "clip"="clip.exe"
abbr "firefox"="firefox-nightly -start-debugger-server"
abbr "fk"="fuck"
abbr "fzm"="cd \"\$(fzf_dirs)\""
abbr "ga"="git add"
abbr "gb"="git branch"
abbr "gbuild"="convco commit --build"
abbr "gc"="git checkout"
abbr "gca"="git commit --amend"
abbr "gcb"="git checkout -b"
abbr "gchore"="convco commit --chore"
abbr "gci"="convco commit --ci"
abbr "gcm"="git commit -m"
abbr "gco"="git checkout"
abbr "gcoo"="git checkout --ours"
abbr "gcot"="git checkout --theirs"
abbr "gcp"="git cherry-pick"
abbr "gcpa"="git cherry-pick --abort"
abbr "gcpc"="git cherry-pick --continue"
abbr "gcps"="git cherry-pick --skip"
abbr "gd"="git diff"
abbr "gD"="git branch -D"
abbr "gdc"="git diff --cached"
abbr "gdocs"="convco commit --docs"
abbr "gds"="git diff --staged"
abbr "gdup"="git diff @{upstream}"
abbr "gf"="git fetch --all --tags"
abbr "gfeat"="convco commit --feat"
abbr "gfix"="convco commit --fix"
abbr "gitzip"="git archive HEAD -o brighten-tompkins.zip"
abbr "gl"="git log --pretty=format:'%h %<(20)%an %s' | nvim -c 'set ft=git'"
abbr "gld"="git log --date=format-local:'%Y-%m-%d %I:%M%p' --pretty=format:'%h %ad %<(20)%an %s' | nvim -c 'set ft=git'"
abbr "gm"="git merge"
abbr "gma"="git merge --abort"
abbr "gmc"="git merge --continue"
abbr "gms"="git merge --skip"
abbr "gore"="rlwrap yaegi"
abbr "gp"="git push --follow-tags"
abbr "gpd"="git push --follow-tags -o ci.variable=\"CI_NO_TEST=true\" -o ci.variable=\"CI_NO_FORMAT=true\" -o ci.variable=\"CI_NO_DOCS=true\""
abbr "gpl"="git pull"
abbr "gplo"="git pull origin"
abbr "gps"="git push --follow-tags -o ci.skip"
abbr "gr"="git remote"
abbr "grb"="git rebase"
abbr "grba"="git rebase --abort"
abbr "grbc"="git rebase --continue"
abbr "grbs"="git rebase --skip"
abbr "grefac"="convco commit --refactor"
abbr "grev"="git revert"
abbr "grgo"="git remote get-url origin"
abbr "grh"="git reset"
abbr "grm"="git rm"
abbr "grs"="git restore"
abbr "grso"="git remote set-url origin"
abbr "gs"="git status"
abbr "gsh"="git show"
abbr "gsmr"="git submodule update --init --recursive"
abbr "gsshp"="git config core.sshCommand \"ssh -i ~/.ssh/id_ed25519_git_personal -F /dev/null\""
abbr "gstyle"="convco commit --style"
abbr "gsup"="git submodule foreach 'git fetch && git checkout main && git rebase origin/main | :'"
abbr "gtest"="convco commit --test"
abbr "gwk"="git worktree"
abbr "gwkad"="git worktree add"
abbr "gwkls"="git worktree list"
abbr "gwkpr"="git worktree prune"
abbr "gwkrm"="git worktree remove"
abbr "kb"="kubectl -n"
abbr "kbd"="kubectl"
abbr "log"="tail -f -n 5"
abbr "lsc"="colorls"
abbr "murder"="killall -s SIGKILL"
abbr "mutt"="neomutt"
abbr "p"="paru -S --noconfirm"
abbr "pdfpng"="pdftoppm -png"
abbr "s"="abbr clear-session && source_files export env functions alias alias-git bindings inputrc fzf.zsh"
abbr "sl"="ls"
abbr "slackt"="slack-term -config ~/.slack"
abbr "tz"="sudo ntpd -qg"
abbr "v4l2reset"="sudo rmmod v4l2loopback"
